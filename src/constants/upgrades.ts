export const MAX_UPGRADE_LEVEL = 5;

// Tunable per-level stat gains
export const speedIncreasePerLevel = 0.15; // +15% engine force per level
export const brakeIncreasePerLevel = 0.2; // +20% braking force per level
export const boostForceIncreasePerLevel = 0.1; // +10% boost power per level
export const boostCapacityIncreasePerLevel = 0.25; // +25% boost capacity per level

// Tunable prices
export const speedUpgradePrice = 150;
export const brakeUpgradePrice = 200;
export const boostUpgradePrice = 300;

// Base stats (before upgrades)
export const baseMaxBoost = 100;
